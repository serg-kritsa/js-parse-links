const path = require('path')

const PATH_TO_REPORT = path.join('users-data', 'report-word-stats.pdf')
const PATH_TO_REPORTS_DIR = path.join('users-data')
module.exports = {
  PATH_TO_REPORT,  
  PATH_TO_REPORTS_DIR
}